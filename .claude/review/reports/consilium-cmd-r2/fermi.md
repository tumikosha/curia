# fermi — раунд 2

РАУНД 2. Все три числовые находки раунда 1 закрыты правкой; проверяю новые числа и сходимость заново.

```
ВХОД
  обещание — цена работы команды в контекстах: «раунд команды стоит порядка
             десяти контекстов, полный цикл — до двадцати»
             (commands/consilium.md, стр. 191–192); плюс пять констант-лимитов
             (≤4 §1/§2, ≤2 §5, «раунд N из 2» §1/§8, «такта три не бывает» §5,
             порог арбитра ≥2 §6)
  бюджет   — двойной, оба названы:
             (а) внешний лимит для констант — TEAM.md, правила 1, 4, 5, 6, 7,
                 Словарик, стр. 100–102 (документ вне проверяемого файла);
             (б) обещание самого плана для цены — «10 / до 20 контекстов»
  направление — потолок (затрата: контексты, вызовы); для констант — потолок
                (лимит не превышать)
  повторный вход — да: раунд 2 из 2; предъявлен замер — фактический прогон
                   раунда 1 (каталог .claude/review/reports/consilium-cmd-r1/,
                   4 файла отчётов судей), это след прогона, а не строка плана

РАЗЛОЖЕНИЕ А. Контекстов за один раунд
  (свёртка: сумма — все вклады занимают бюджет одного раунда совместно;
   размерность: контексты + контексты = контексты, сходится)
  триаж `larrey` — [1 – 3] контекста — основание: объём работы задан планом
             (§2 «roster отложен» → повторный вызов, §3 «поднял лимит» →
             повторный вызов; оба внутри того же раунда); частота этих веток
             источника не имеет — вилка конвенцией, подтвердить
  такт 1 — [1 – 5] — якорь-диапазон: TEAM.md прав. 1 (≤4 судьи), низ —
             одиночный вход (TEAM.md стр. 91–93); верх 5, а не 4: второй ход
             `reverse-spec-reviewer` (§4, стр. 83–88) — лишний вызов при том
             же составе 4
  такт 2 — [0 – 2] — якорь: TEAM.md прав. 4 (≤2 агентов), низ — передач нет
  свод + допрос — [0 – 5] — подразложение (сумма): арбитр [0 – 1]
             (TEAM.md прав. 6) + допрос судей [0 – 4] (§5 стр. 131–133
             легализует допрос вне лимита тактов; источник — TEAM.md стр. 213
             «право допроса ревьюеров»); верх допроса — прикидка, ограничена
             сверху размером состава, снизу нулём
  правки исполнителя (+напарник) — [0 – 2] — прикидка: §8 и «Границы»
             («спавни отдельного напарника» — один), низ — правок нет
  свёртка — [2 – 17] контекстов за раунд
  ЗАМЕР раунда 1 этого же ревью — 4 судьи (файлы в consilium-cmd-r1/) + триаж
             + свод + переписывание артефакта ≈ 7–9 контекстов
  × раундов [1 – 2] (TEAM.md прав. 7) — цикл [4 – 34]; фактически r1+r2 ≈ 16–19

РАЗЛОЖЕНИЕ Б. Составной вход (§2, стр. 40–44) — надбавка
  (свёртка: сумма — два roster'а исполняются последовательно, но в одном
   бюджете двух раундов; размерность сходится)
  второй такт 1 — [1 – 5] + второй такт 2 — [0 – 2] + второй свод — [0 – 1]
  свёртка надбавки — [1 – 8] контекстов на раунд → цикл составного входа
             [6 – 34+], ожидаемо ~26–30 при полном составе обоих roster'ов

ЯКОРЯ
  «10 × 2 раунда = 20» — внутренняя арифметика самого утверждения:
    **согласуется**; в раунде 1 этой пары чисел в файле не было вовсе, теперь
    она перемножена
  замер r1 (4 файла отчётов) против константы ≤4 (TEAM.md прав. 1):
    **согласуется** — прогон уложился в назначенный состав
  пять констант против источника, дословная сверка:
    ≤4 (§1 «по умолчанию 4», §2, §4) = TEAM.md прав. 1 + Роли + Словарик
      («вызывающий волен поднять лимит» ↔ §3 «поднять лимит волен и ты сам») —
      **согласуется**
    ≤2 передач (§5) = TEAM.md прав. 4 — **согласуется**
    «раунд N из 2», счётчик у вызывающего, одиночное ревью — тоже раунд,
      повторный вход не сбрасывает (§1, §8) = TEAM.md прав. 7 —
      **согласуется дословно, все четыре условия**
    «такта три не бывает» (§5) = Словарик «тактов максимум два» + прав. 5 —
      **согласуется**
    порог арбитра «два отчёта и больше» (§6) = TEAM.md прав. 6 «≥2 отчётов»;
      «единственный отчёт идёт владельцу как есть» = TEAM.md стр. 94 —
      **согласуется**
  составной вход (§2) против TEAM.md стр. 100–102 («два входа, каждому свой
    состав… общий лимит двух раундов»): **согласуется**, и разрешает
    кажущийся конфликт с прав. 1 верно — не «8 в первом такте», а два такта 1
    по ≤4 по очереди; размерность лимита (агенты на такт) сохранена
  внутренние счётные заявления: §1 «три текста, одно число и контекст
    исполнения» → 5 пунктов, ровно 5 в списке; §2 «одним из пяти исходов» →
    5 пунктов — **оба согласуются** (счётные заявления, которые сходятся)

ВЕРДИКТ — **сходится**. Заявление о цене прочитано как заявление о порядке
  (слова «порядка» и «до» это и говорят): вилка раунда [2 – 17] накрывает
  заявленные 10, вилка цикла [4 – 34] накрывает заявленные 20, замер живого
  прогона (r1 ≈ 8–9; r1+r2 ≈ 16–19) ложится ровно в обе точки. Пять
  констант-лимитов воспроизводят TEAM.md без единого расхождения.

  **впритык** — при буквальном чтении «до двадцати» как потолка: замеренный
  цикл 16–19 против 20 даёт запас ×1.05, и два фактора съедают его первыми:
  (1) составной вход (§2) — надбавка до ×1.5–2 к циклу, «до двадцати» её не
  покрывает; (2) допрос судей арбитром (§5, стр. 131–133) — до +4 контекстов
  на раунд, легализован текстом и в цене не учтён. Оба занижают, а не
  завышают: аргумент «мелкий вход мимо команды» от этого крепнет (a fortiori),
  поэтому это заметка, а не blocker. Точная формулировка, снимающая её:
  «полный цикл — до двадцати, составной вход — вдвое дороже».

  закрытие находок раунда 1:
    1. «четыре контекста» → «порядка десяти контекстов, полный цикл — до
       двадцати» (стр. 191–192) — **закрыто**, число теперь перемножено
       и совпадает с замером;
    2. непокрытый составной вход → §2, ветка «два roster'а» с собственным
       тактом 1 ≤4, собственным сводом и общим лимитом двух раундов —
       **закрыто**, расхождение с TEAM.md стр. 100–102 снято;
    3. пропускная такта 2 против спроса → §5, стр. 123–125: «первые две в
       порядке отчётов… остаток в непокрыто. Выбор здесь механический» —
       **закрыто**; правило отбора шире источника (TEAM.md прав. 5 молчит о
       том, какие две), но не противоречит ему и повторяет принцип
       фиксированного порядка из прав. 1;
    4. неоднородность единицы «раунд» (2 контекста и 17 тратят одну единицу
       бюджета «2 раунда») — **осталась**, но она унаследована от TEAM.md
       прав. 7 дословно; артефакт воспроизводит источник верно, чинить надо
       протокол, а не команду.

  пометка точности — **нет**. «Порядка» и «до» — записи о порядке, вторая
  значащая цифра не заявлена; пять констант — назначенные лимиты, а не
  прикидки, к ним пометка неприменима.

  самое дорогое утверждение отчёта — замер «раунд ≈ 7–9 контекстов»: в
  каталоге consilium-cmd-r1/ лежат только 4 файла судей, вызовы триажа и свода
  я досчитал по протоколу, а не наблюдал (вердикта арбитра r1 в каталоге нет).
  чем оно опровергается — вызывающий называет фактическое число вызовов агентов
  за раунд 1 из своего журнала сессии; результат сузит вилку раунда до точки и
  скажет, «десять» ли там или «пятнадцать».
```

Файлы: `consilium/commands/consilium.md` (стр. 40–44 составной вход, 83–88 второй ход reverse-spec, 123–125 переполнение передач, 131–133 допрос арбитром, 191–192 цена цикла), `consilium/TEAM.md` (прав. 1, 4, 5, 6, 7, Словарик, стр. 91–94, 100–102, 213 — бюджет констант).

— `fermi`
