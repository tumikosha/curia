# contract-reviewer — раунд 2

## Раунд 1 — статус моих замечаний

| # | замечание р1 | статус | где закрыто |
|---|---|---|---|
| 1 | [blocker] ветка «владелец поднял лимит» аннулирована | **исправлено** | `commands/consilium.md:62-65` — исходы разведены: «поднял лимит» → повторный вызов larrey и расширенный состав; «подтвердил, промолчал, автономный» → назначенный состав + «непокрыто» |
| 2 | [major] не-roster исходы триажа | **исправлено** | `:34-50` — все пять исходов larrey (`larrey.md:257-278`) на месте, включая ОТКАЗ и «roster отложен» |
| 3 | [minor] счётчик раундов | **исправлено** | `:25-28` («повторный вход даёт N+1»), `:176-178` (номер идёт триажу и арбитру) |
| 4 | [minor] обход по «мелкой правке» | **исправлено** | `:190-191` |

## Новые замечания (раунд 2, фокус: семь правил «Протокола», таблицы, «Портируемость»)

```
[major] Фазе B назначен блокирующий вердикт, который в спеке арбитра означает «blocker'ов нет»
где:        consilium/commands/consilium.md:141-143 (и следствия :42-43, :163)
что ломает: составной вопрос «туда ли копаем — и заодно проверь готовое» (фазы B+C, порядок по
            larrey.md:78-81). Каллер передаёт арбитру «фаза B — ПЕРЕСМОТРЕТЬ РАЗБОР» в одном ряду
            с блокирующими словами ВЕРНУТЬ АВТОРУ (C) и НЕ НАЧИНАТЬ (A). По review-arbiter.md:28
            ПЕРЕСМОТРЕТЬ РАЗБОР — вторая ступень, парная к «НАЧИНАТЬ С ЗАМЕЧАНИЯМИ», то есть
            «blocker'ов нет» (:25). У фазы B первой ступени в спеке арбитра слова нет вовсе.
            Итог: ревью фазы B с blocker'ами выходит владельцу под неблокирующим словом (§7:161
            велит показать вердикт дословно), а §2:42-43 останавливает поздние ревью только по
            двум словам — НЕ НАЧИНАТЬ и ВЕРНУТЬ АВТОРУ, — поэтому ревью фазы C оплачивается
            вопреки larrey.md:80 «отрицательный вердикт ранней фазы делает поздние ревью ненужными».
            Побочно: «ВЕРНУТЬ АВТОРУ ранней фазы» — мёртвая ветка, C всегда последняя.
проверка:   grep -rn "ПЕРЕСМОТРЕТЬ\|РАЗБОР ДЕРЖИТСЯ\|НАЧИНАТЬ С ЗАМЕЧАНИЯМИ" TEAM.md agents/ commands/
            — слово определено ровно в одном месте, review-arbiter.md:28, и там оно на второй ступени.
фикс:       :141-143 — не диктовать слово, а передавать фазу: «фазу входа — словарь вердикта арбитр
            берёт из своей спеки». :42-43 — «блокирующий вердикт ранней фазы (первая ступень её
            словаря) закрывает поздние ревью» вместо перечня двух слов.
```

```
[major] Каталог отчётов <имя-прогона>/ расходится с путями, зашитыми во все спеки судей и арбитра
где:        consilium/commands/consilium.md:171
что ломает: судьи пишут не туда, куда велит команда: contract-reviewer.md:75 →
            `.claude/review/reports/contract-reviewer.md`, reverse-spec-reviewer.md:43,45,
            premortem-reviewer.md:47 — тоже плоско; арбитр читает `.claude/review/reports/*.md`
            (review-arbiter.md:36) и держит `_arbiter-state.md` там же (:48). TEAM.md:213 называет
            именно плоский `.claude/review/reports/`. Реальное состояние репозитория показывает
            последствие уже сейчас: в плоском каталоге лежат отчёты ЧУЖОГО прогона (ревью
            репозитория, 30 авг 13:33 и 13:42), а отчёты текущего прогона — в consilium-cmd-r1/
            (18:51-18:55). Арбитр раунда 2, исполнив своё правило «читай отчёты из *.md», прочтёт
            протухшие отчёты о другом артефакте и не найдёт отчётов этого раунда; по его же
            правилу :46 отсутствие отчёта = `ПРОВЕРКА НЕ ПРОВЕДЕНА`.
проверка:   ls -la .claude/review/reports/ .claude/review/reports/consilium-cmd-r1/ ;
            head -1 .claude/review/reports/contract-reviewer.md  → «раунд 1 (Consilium, ревью
            репозитория world_agents)», то есть другой прогон;
            grep -rn "review/reports" agents/ commands/ TEAM.md
фикс:       :171 — рабочий каталог оставить плоским (`.claude/review/reports/`, как в TEAM.md:213
            и в спеках судей), а перед тактом 1 архивировать отчёты прошлого прогона в
            `<имя-прогона>/`; либо, если подкаталог нужен, поправить его во всех спеках — но это
            уже не правка команды.
```

```
[minor] Каллер ждёт от reverse-spec строку, которой тот в этом режиме не выдаёт
где:        consilium/commands/consilium.md:86-87
что ломает: команда велит делать второй ход «после его строки "РЕКОНСТРУКЦИЯ ЗАВЕРШЕНА"».
            Эта строка — из портируемого режима (reverse-spec-reviewer.md:23). В режиме
            субагента, который команда и использует, агент возвращает
            `reverse-spec | РЕКОНСТРУКЦИЯ ГОТОВА | жду настоящее ТЗ` (:43). Каллер, сверяющий
            буквально, второго хода не делает — такт 1 остаётся без diff'а, а бюджет уже потрачен.
проверка:   diff строк reverse-spec-reviewer.md:23 и :43 с consilium.md:86-87
фикс:       :86 — «после его строки о готовности реконструкции (`РЕКОНСТРУКЦИЯ ГОТОВА` в режиме
            субагента, `РЕКОНСТРУКЦИЯ ЗАВЕРШЕНА` в портируемом)».
```

```
[minor] Каллеру разрешено самому поднять лимит там, где протокол велит исполнить назначенный состав
где:        consilium/commands/consilium.md:67-69
что ломает: правило 2 TEAM.md:137-138 перечисляет исходы паузы закрыто: «подтвердил, промолчал,
            режим автономный — исполняется назначенный состав» (то же larrey.md:50-51). Команда
            добавляет каллеру право поднять лимит именно в автономном режиме, по вкусовому
            критерию «вход того стоит». Автономный режим — основной для слэш-команды, вызванной
            главным агентом, так что ≤4 из правила 1 становится рекомендацией, и решение о составе
            принимает тот, кого никто не проверяет. Формально опора есть (TEAM.md:42 «вызывающий
            волен поднять лимит»), но это словарик, а не правило 2 — решение принято молча.
проверка:   сверка consilium.md:67-69 с TEAM.md:132-138 и larrey.md:47-52
фикс:       :67-69 — ограничить веткой «владельца у пульта нет и пауза невозможна» с обязательной
            строкой в своде: «лимит поднят вызывающим — кто добавлен и почему»; либо убрать.
```

```
[minor] «Единственное исключение из запрета судить» опровергается на 160 строк ниже
где:        consilium/commands/consilium.md:9 против :168-169
что ломает: :9 — «Судить самому запрещено; единственное исключение названо в §6» (ручной свод).
            :168 разрешает каллеру приписать к вердикту своё несогласие с blocker'ом «прямо и с
            аргументом» — это второе исключение, и оно противоречит и :161-162 («своё резюме
            поверх сюда не идёт»), и правилу 6 TEAM.md:154-155 «вердикт арбитра — единственный
            голос команды». Владелец получает два голоса, причём второй — от того, кто заказывал
            работу исполнителю.
проверка:   TEAM.md:154-156 и :22-23 (роль вызывающего — запускает и передаёт, не судит)
фикс:       :168 — «несогласие с blocker'ом адресуй арбитру до вердикта (право допроса — его),
            а не владельцу поверх вердикта»; либо назвать это исключение в :9 явно.
```

## Шаг 3 — дельта

- **Сверх источника, объявлено и безвредно**: `:123-125` приоритет при >2 передач («первые две в порядке отчётов») — в TEAM.md и larrey.md умолчание; выбран механический критерий, судейства не вносит. `:87-88` «оба хода reverse-spec принадлежат такту 1 и бюджет передач не тратят» — в источнике не сказано; чтение разумное.
- **Умолчание не задано, решение молчит**: инкрементируется ли счётчик при повторном вызове larrey с поднятым лимитом. Для «roster отложен» это оговорено (`:48` «счётчик не двигается»), для ветки лимита (`:62`) — нет; по контрасту каллер может списать раунд.
- **Умолчание не задано**: `:113-119` вход такта 2 не содержит вопроса владельца (в такте 1 он есть, `:76-81`). Источник вход такта 2 не описывает; последствие мягкое, отдельным замечанием не поднимаю.
- Портируемость (TEAM.md:199-217) сверена: последовательные вызовы и свежая сессия на судью — `:100-105`; свод последним вызовом со всеми отчётами — `:138-140`; арбитр недоступен → «свод вручную» — `:154-157`. Расхождение одно, и оно в замечании про каталог.

## Что проверял фактами

- TEAM.md целиком (218 строк); семь правил «Протокола» построчно против §1-§8 команды; обе таблицы (фазы/база и триггеры) — против `larrey.md:84-114`.
- `agents/larrey.md`: контракт входа (:242-255), правило вердикта (:257-278), такты (:191-221), составной вопрос (:74-82), носитель/замещение (:176-189), формат roster (:296-332).
- `agents/review-arbiter.md` целиком; `agents/reverse-spec-reviewer.md` целиком.
- `grep -rn "review/reports"` по agents/, commands/, TEAM.md — пять мест, все плоские, кроме `consilium.md:171`.
- `ls -la .claude/review/reports/**` + `head` протухших файлов — коллизия прогонов воспроизведена на реальном состоянии репозитория.
- `grep -rn "ПЕРЕСМОТРЕТЬ|РАЗБОР ДЕРЖИТСЯ|НЕ НАЧИНАТЬ|НАЧИНАТЬ С ЗАМЕЧАНИЯМИ"` — словарь фаз определён ровно в одном месте.
- Не смог проверить: фактический прогон команды (нет прав на исполнение); все выводы — сверка инструкций с контрактами спек.

**BLOCKERS: 0** (все четыре замечания раунда 1 закрыты; новых blocker'ов нет, открыты 2 major и 3 minor — до их разбора это не «ПРИНЯТО»).

`contract-reviewer | раунд 2 | BLOCKERS: 0 | новые: 2 major (словарь вердикта фазы B; каталог отчётов <имя-прогона>/ против плоского пути во всех спеках), 3 minor | проверено: 7 правил Протокола, обе таблицы, Портируемость, larrey.md, review-arbiter.md, reverse-spec-reviewer.md, реальный .claude/review/reports/`
