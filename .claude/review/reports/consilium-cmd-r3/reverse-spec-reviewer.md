# reverse-spec — раунд 3, ход 2: diff реконструкции против настоящего ТЗ

ТЗ: `consilium/TEAM.md`. Артефакт: `consilium/commands/consilium.md`.

**Пометка о загрязнении (перенесена из хода 1):** в контекст сессии автоматически подгружена память проекта (`MEMORY.md`) со строками о Consilium, `larrey`, `TEAM.md` и «двухфазном входе reverse-spec». Файлов репозитория до сдачи реконструкции я не открывал, но эти строки прочитать не мог не успеть. Затронуты пункты, помеченные (*). Текста `TEAM.md` в памяти не было — совпадения по протоколу получены из артефакта.

**Что совпало (сверено по пунктам ТЗ).** Роли и их несмешение; `larrey` назначает и не запускает; лимит первого такта ≤4; две категории причин отреза и запрет рационализации поверх; поимённый список отрезанных; пауза при ёмкостном отрезе до исполнения, три пассивных исхода, отрезанные в «непокрыто»; возврат ёмкостно отрезанного передачей и строка свода об этом; изоляция такта и вход «артефакт целиком + указание что нужно его вопросу, без пересказов»; такт 2 по строкам перенаправления, ≤2, легальность повторного вызова участника такта 1; остановка после второго такта и формулировка «непокрыто — отдельным вызовом по решению владельца»; `review-arbiter` обязателен при ≥2 отчётах и его вердикт — единственный голос; одиночный вход и переход к обязательному арбитру после передачи; составной вопрос — два входа, порядок A→B→C, общий лимит двух раундов; повторный вход по дельте, полный состав при переписанном артефакте, два раунда и эскалация владельцу; счётчик у вызывающего, номер в roster и арбитру, одиночное ревью тоже раунд, повторный вход не сбрасывает; портируемость через последовательные вызовы и свежую сессию на судью; ручной свод по спеке арбитра с пометкой «свод вручную».

---

### ПРОПУЩЕНО

| # | Требование ТЗ | Статус в артефакте | Severity | Последствие |
|---|---|---|---|---|
| P1 | «В портируемой модели арбитр получает все отчёты **одним входом**» (раздел «Портируемость») | Отсутствует. §6 знает единственный способ передачи — «все отчёты поимённо путями»; портируемой ветки у свода нет, хотя §1 и §4 её имеют | **major** | В harness без чтения файлов свод получает пути в пустоту: арбитр сводит по нулю отчётов либо прогон встаёт на последнем шаге |
| P2 | «Вместо допроса арбитр помечает недостающее «непокрыто»» — допрос есть опция богатого harness | Отсутствует как правило. §5 описывает допрос как штатный механизм, §7 компенсирует только случай «арбитр уже закрыт» | minor | В портируемом прогоне вызывающий ждёт допроса, которого не будет, и добирает недостающее сам — то есть судит |
| P3 | «Вызывающий — главный агент сессии **или человек**» | Отсутствует. Весь артефакт написан под агента-вызывающего плюс отдельного человека-владельца | minor | Безвредно для формата слэш-команды, но роль «человек ведёт прогон руками по этому файлу» не обслужена: ветки «владельца у пульта нет» в ней бессмысленны |

---

### ДОДУМАНО

| # | Что добавлено | Оценка | Severity |
|---|---|---|---|
| D1 | **Исходное задание дословно** как пятый обязательный элемент входа, в шаблонах такта 1, такта 2 и в своде, плюс ветка «задание отсутствует». ТЗ определяет вход судье как «исходный артефакт целиком плюс указание, что нужно его вопросу» — задания там нет | Безобидное расширение, и я считаю его **неявно требовавшимся**: без ТЗ `contract-reviewer` и `reverse-spec-reviewer` из таблицы триггеров не могут исполнить свой вопрос. Объём (дословность, отдельная ветка отсутствия) — авторский | minor |
| D2 (*) | Двухходовой `reverse-spec-reviewer`: первый вызов без задания и вопроса, ограничение области чтения путями артефакта, две сигнальные строки для двух режимов | Расширение вне ТЗ: `TEAM.md` знает этот агент только строкой триггера. Правило необходимое (иначе агент объявляет реконструкцию загрязнённой), но протокол о нём молчит — дыра в ТЗ, а не в артефакте | minor |
| D3 | Вопрос владельца уходит **только носителю вопроса**, остальные его не видят | Безобидное расширение, согласованное с духом изоляции; ТЗ такого запрета не содержит, но и обратного не требует | minor |
| D4 | Пять исходов триажа, включая «roster отложен» (с презумпцией фазы C со второго захода) и «ОТКАЗ» при пустом `$ARGUMENTS` | Расширение из спеки `larrey`; ТЗ перечисляет только roster, одиночный и составной | minor |
| D5 | Механический отбор первых двух передач в порядке roster'а; вторая обязательная строка триажа «вопрос владельца не судит никто — почему»; закрытый список из четырёх блокирующих вердиктов | Все три отсутствуют в ТЗ. Первое — корректная доводка правила 5; второе и третье заимствованы из чужих спек, при этом §6 сам же запрещает вызывающему диктовать арбитру словарь и тут же перечисляет закрытый список слов | minor |
| D6 | Ротация отчётов прошлого прогона в подкаталог `<имя-прогона>/` перед тактом 1 | Безобидное расширение, чинит реальный отказ (арбитр читает чужой прогон) | minor |
| D7 | Строка несогласия вызывающего с blocker'ом после вердикта | Ближайший кандидат в подмену задачи: ТЗ объявляет вердикт арбитра **единственным голосом команды**. Я оцениваю как расширение, а не подмену — голос отдаётся владельцу отдельной строкой, дословный blocker остаётся на месте, и это не голос команды, а рапорт исполнителя своему человеку | minor |
| D8 | «Границы»: судьи read-only, отдельный напарник-исполнитель, порог «вход слишком мелкий — спроси владельца», цена прогона в контекстах; блокирующий вердикт ранней фазы закрывает поздние фазы составного входа; отдельный свод на каждую фазу | Расширения. Все безобидны; последнее (блокер ранней фазы закрывает поздние) — самостоятельное процессуальное решение, экономящее раунд, но ТЗ его не давало | minor |

---

### РАЗОШЛОСЬ

| # | Пункт | В ТЗ | В артефакте | Severity | Последствие |
|---|---|---|---|---|---|
| R1 | Кто поднимает лимит после ёмкостного отреза | Словарик: «**вызывающий** волен поднять лимит»; правило 2: «**владелец** поднимает лимит или подтверждает» — само ТЗ противоречиво | §3 запрещает вызывающему трогать лимит после триажа: «менять его в обход паузы значит решать, чей вопрос стоит контекста, а это суд» | minor | В автономном прогоне состав навсегда ≤4, отрезанные всегда уходят в «непокрыто»; противоречие в ТЗ снято молча, без пометки |
| R2 | Обязательная строка свода | Дословно: «вопрос **вызывающего** судится замещённым составом — кем» | Дословно: «вопрос **владельца** судится замещённым составом — кем» | minor | Строка `larrey`/арбитра и строка вызывающего не совпадают текстуально: в вердикте окажутся две формулировки одного требования, сверка по строке ломается |
| R3 | Каталог `.claude/review/reports/` | Опция богатого harness внутри teammate-протокола спеки `review-arbiter` | §7 предписывает его безусловно, для всех режимов | minor | В портируемом harness вызывающий тратит шаг на файловую дисциплину, которой протокол там не требует |
| R4 | Фаза при незаданном вопросе | «Вопрос не задан — по состоянию работы» | §2: со второго захода `larrey` работает «по презумпции фазы C» | minor | План, поданный без вопроса, получает C-состав (`contract-reviewer` + `adversarial`) вместо A — судьи проверяют готовность у ненаписанного |
| R5 | Определение такта | «Такт — один заход: назначенные работают **параллельно** и сдают отчёты» | §4: оба хода reverse-spec принадлежат такту 1 и бюджет передач не тратят | minor | Такт перестаёт быть одним параллельным заходом; правило удобно, но выдано самим артефактом, а не протоколом — при споре о бюджете сослаться не на что |

---

**РАСХОЖДЕНИЙ: 16**

- **major (1):** P1 — портируемый свод без передачи отчётов одним входом.
- **minor (15):** P2, P3, D1, D2(*), D3, D4, D5, D6, D7, D8, R1, R2, R3, R4, R5.
- **blocker: 0.**

Общая оценка: артефакт покрывает протокол `TEAM.md` практически целиком и точнее его самого в местах, где протокол противоречив (R1) или молчит (D5, D6). Единственная содержательная потеря — портируемый режим свода: §1 и §4 его помнят, §6 забыл.
