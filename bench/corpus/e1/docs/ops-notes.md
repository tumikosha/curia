# Заметки по эксплуатации returns

- 2025-11: включена архивация. Таблица `returns` держит последние
  90 дней, старше — в `returns_archive` (холодное хранилище).
  До архивации в `returns` было ~2,1 млн строк, сейчас — порядка
  60–70 тыс.
- Суточный объём возвратов: 600–900 записей.
- Дашборд возвратов (`dashboard/views.py`) читает агрегаты по
  магазинам напрямую из БД, p95 ответа 180 мс.
- Старый экспорт для ACME (`legacy/acme_export.py`) удалён 2026-04,
  вместе с ключами SFTP.
