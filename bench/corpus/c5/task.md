# Задание

Лимитер для публичного API.

- Квота: 100 запросов в минуту **на каждый API-ключ**; ключ приходит
  в заголовке `X-Api-Key`.
- Поверх квоты — burst до 20 запросов подряд (token bucket).
- Превышение — HTTP 429 с заголовком `Retry-After` (секунды до
  следующего доступного слота).
